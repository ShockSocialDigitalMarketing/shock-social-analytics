<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Social Media Investment Impact</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/3.9.1/chart.min.js"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Assistant:wght@200;300;400;500;600;700;800;900&display=swap');
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Assistant', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            font-weight: 400;
            background: linear-gradient(135deg, #000000 0%, #1a1a1a 100%);
            color: white;
            min-height: 100vh;
            padding: 20px;
            margin: 0;
        }

        .dashboard {
            max-width: 1200px;
            margin: 0 auto;
            padding-top: 20px;
        }

        .comparison-section {
            background: linear-gradient(145deg, #1a1a1a, #2a2a2a);
            border-radius: 20px;
            padding: 40px;
            border: 1px solid rgba(50, 211, 251, 0.2);
            box-shadow: 0 10px 30px rgba(50, 211, 251, 0.1);
        }

        .comparison-title {
            text-align: center;
            font-size: 28px;
            font-weight: 800;
            color: #32D3FB;
            margin-bottom: 30px;
        }

        .chart-container {
            position: relative;
            height: 300px;
            margin-bottom: 30px;
        }

        .before-after {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 30px;
            margin-top: 30px;
        }

        .before, .after {
            text-align: center;
            padding: 20px;
            border-radius: 15px;
        }

        .before {
            background: rgba(255, 0, 0, 0.1);
            border: 1px solid rgba(255, 0, 0, 0.3);
        }

        .after {
            background: rgba(50, 211, 251, 0.1);
            border: 1px solid rgba(50, 211, 251, 0.3);
        }

        .period-label {
            font-size: 16px;
            font-weight: 700;
            margin-bottom: 15px;
        }

        .impact-number {
            font-size: 36px;
            font-weight: 900;
            margin: 10px 0;
        }

        .growth-indicator {
            font-size: 14px;
            display: inline-flex;
            align-items: center;
            gap: 5px;
            background: rgba(50, 211, 251, 0.2);
            padding: 5px 15px;
            border-radius: 20px;
            font-weight: 700;
            color: #32D3FB;
            margin-top: 10px;
        }

        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }

        .pulse {
            animation: pulse 2s infinite;
        }

        @media (max-width: 768px) {
            .before-after {
                grid-template-columns: 1fr;
            }
            
            .comparison-section {
                padding: 20px;
            }
            
            .comparison-title {
                font-size: 24px;
            }
        }
    </style>
</head>
<body>
    <div class="dashboard">
        <!-- Response Time Comparison -->
        <div class="comparison-section">
            <div class="comparison-title">Social Media Investment Impact</div>
            <div class="chart-container">
                <canvas id="responseChart"></canvas>
            </div>
            <div class="before-after">
                <div class="before">
                    <div class="period-label" style="color: #ff6b6b;">WITHOUT SOCIAL MEDIA MANAGER</div>
                    <div class="impact-number" style="color: #ff6b6b;">2-4 HOURS</div>
                    <div>Average Response Time</div>
                </div>
                <div class="after">
                    <div class="period-label" style="color: #32D3FB;">WITH SOCIAL MEDIA MANAGER</div>
                    <div class="impact-number" style="color: #32D3FB;">2-5 MINUTES</div>
                    <div>Average Response Time</div>
                    <div class="growth-indicator">
                        ⚡ 95% IMPROVEMENT
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Chart.js configuration
        Chart.defaults.color = '#ffffff';
        Chart.defaults.plugins.legend.labels.color = '#ffffff';

        const brandColors = {
            primary: '#32D3FB',
            secondary: '#000000',
            accent: '#ffffff',
            gradient: ['#32D3FB', '#1a9cc9', '#0f7599']
        };

        // Response Time Comparison Chart
        const responseChart = new Chart(document.getElementById('responseChart'), {
            type: 'line',
            data: {
                labels: ['Week 1', 'Week 2', 'Week 3', 'Week 4', 'Week 5', 'Week 6'],
                datasets: [
                    {
                        label: 'Without Social Media Manager',
                        data: [180, 165, 190, 175, 185, 170],
                        borderColor: '#ff6b6b',
                        backgroundColor: '#ff6b6b30',
                        borderWidth: 3,
                        tension: 0.4,
                        fill: true
                    },
                    {
                        label: 'With Social Media Manager',
                        data: [15, 8, 5, 3, 4, 2],
                        borderColor: brandColors.primary,
                        backgroundColor: brandColors.primary + '30',
                        borderWidth: 3,
                        tension: 0.4,
                        fill: true
                    }
                ]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: {
                        position: 'top',
                        labels: { padding: 20, font: { size: 14 } }
                    }
                },
                scales: {
                    y: {
                        beginAtZero: true,
                        grid: { color: '#333' },
                        title: {
                            display: true,
                            text: 'Response Time (Minutes)',
                            color: '#fff'
                        }
                    },
                    x: {
                        grid: { color: '#333' }
                    }
                },
                interaction: {
                    intersect: false,
                    mode: 'index'
                }
            }
        });

        // Add animation effects
        document.addEventListener('DOMContentLoaded', function() {
            const section = document.querySelector('.comparison-section');
            section.style.opacity = '0';
            section.style.transform = 'translateY(30px)';
            section.style.transition = 'all 0.8s ease';
            
            setTimeout(() => {
                section.style.opacity = '1';
                section.style.transform = 'translateY(0)';
            }, 200);
        });
    </script>
</body>
</html>
